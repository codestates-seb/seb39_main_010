spring:
  profiles:
    active: local
    include: oauth
  config:
    domain: ${{ secrets.AWS_S3_Endpoint }}
---
spring:
  config:
    activate:
      on-profile: local
---
spring:
  config:
    activate:
      on-profile: server